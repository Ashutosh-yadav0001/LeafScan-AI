# Frontend - React App

This folder contains the website code built using React.js.

## Files
*   `src/home.js`: This is the main page where the user uploads the image. It handles the API call to the backend.
*   `package.json`: Contains the list of packages and the proxy setup (to talk to the backend).

## How to run
Run `npm start` in this folder to start the website.
